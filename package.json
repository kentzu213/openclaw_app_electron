{
  "name": "openclaw",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw - Extension Marketplace Platform connected to IzziAPI",
  "scripts": {
    "dev": "pnpm --filter @openclaw/desktop dev",
    "dev:marketplace": "pnpm --parallel --filter @openclaw/marketplace-api --filter @openclaw/desktop dev",
    "build": "pnpm --filter @openclaw/desktop build",
    "build:all": "pnpm -r build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
